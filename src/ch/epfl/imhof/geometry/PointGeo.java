package ch.epfl.imhof.geometry;
import java.lang.Math;

public final class PointGeo {
	
	/*
	private final double longitude;
	private final double latitude;
	
	public PointGeo (double longitude, double latitude) throws IllegalAccessException {
		
		if (latitude < (java.lang.Math.PI)/2 || latitude > (java.lang.Math.PI)/2){
			throw new IllegalAccessException();
		}
		
		if (longitude < (java.lang.Math.PI) || longitude > java.lang.Math.PI){
			throw new IllegalAccessException();
		}
		
		
		this.latitude=latitude;
		this.longitude=longitude;
	}
	
	public double longitude (){
		return longitude;
		}
	
	public double latitude(){
		return latitude;
	}
	*/
}
